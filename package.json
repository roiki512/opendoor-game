{
  "name": "opendoor-faster",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/gen-og.mjs && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sharp": "^0.35.1",
    "typescript": "~5.7.0",
    "vite": "^6.0.0"
  }
}
